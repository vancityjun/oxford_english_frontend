# import '../fragment/definition.gql'

query Vocabularies($levels: [String]) {
  vocabularies(levels: $levels) {
    id
    word
    level
    pos
    ox5000
    note {
      updatedAt
      definitions {
        ...DefinitionFields
      }
    }
  }
}
